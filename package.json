{
  "name": "norma-monorepo",
  "private": true,
  "type": "module",
  "engines": { "bun": ">=1.3.0" },
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 test",
    "protocol:generate": "pnpm --filter @norma/protocol generate"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  },
  "workspaces": ["packages/*", "spikes/*", "examples/*"]
}
