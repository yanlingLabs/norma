{
  "id": "battery-limiter",
  "name": "Battery Limiter",
  "description": "Reference Tier-2 (platform) plugin for @norma/plugin-sdk's ctx.hardware() — sets/reads the battery charge limit through Norma.app's privileged XPC helper (Phase 4c, design spec §5).",
  "version": "0.0.1",
  "tier": "platform",
  "permissions": { "exec": true, "hardware": ["battery"] },
  "contributes": { "tools": true, "tile": true },
  "entry": { "command": "bun", "args": ["index.ts"] }
}
